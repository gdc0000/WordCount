"""WordCount Statistics — dataset + vocabulary -> enhanced dataset + analytics.

Pure core business logic with FastAPI connectors. The :mod:`wordcount.core` package is
framework-free (no Streamlit, no Fastapi, no Pydantic); :mod:`wordcount.api` is a thin
connector layer exposing the core over HTTP.
"""

__version__ = "0.1.0"
